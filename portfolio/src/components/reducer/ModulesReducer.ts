import { Types } from "./Types";
const InitialState = {
    storeEnabled: false
}
export const ApplicationReducer = (state: any, action: any) => {
    switch (action.type) {
        case Types.STORE_ENABLED:
        // return { ...InitialState: state, storeEnabled: action.payload }
    }


}